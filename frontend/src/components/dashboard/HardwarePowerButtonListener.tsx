"use client";

import React from "react";

export const HardwarePowerButtonListener: React.FC = () => {
  // Keypress listener disabled per user directive - SOS alerts are triggered exclusively via UI buttons & Voice Assistant
  return null;
};
